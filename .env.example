# Copy to .env and adjust for your environment.
# Only DJANGO_DEBUG is shown with a sane default; the rest are optional.

DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1